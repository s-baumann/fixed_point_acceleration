[package]
name = "fixed_point_acceleration"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = "0.15"
linfa-linalg = "0.1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"